
// next.config.js
const nextConfig = {
  // 1. Sets the URL prefix for all links (important for navigation)
  basePath: '/portfolio', 

  // 2. Makes sure assets (JS, CSS, images) are referenced with a relative path
  assetPrefix: './', 

  // ... other settings (output: 'export', images: { unoptimized: true })
};
